/**
 * Limites alinhados com Vercel serverless (~4.5 MB de body por função).
 * Valores canónicos em uploadConstants.js — reexportados aqui para imports legados.
 */

export {
  VIDEO_VERCEL_SAFE_BYTES,
  MAX_VIDEO_USER_BYTES,
} from "./uploadConstants";
