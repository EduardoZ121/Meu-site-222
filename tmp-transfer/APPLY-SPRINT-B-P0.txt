# Aplicar Sprint B P0 (+ harden) em Site_Angola

Pré-requisito: Sprint A (`bf98a700` / `kuteka-sprint-a-p0.patch`) já aplicado na branch de destino.

## Opção 1 — patch B completo (inbox + complaint bridge + harden)
```bash
cd /path/to/Site_Angola
git checkout -b cursor/sprint-b-feedback-inbox-f96b
git apply tmp-transfer/kuteka-sprint-b-p0.patch   # ou path do ficheiro copiado
# revisar, testar, commit se apply --index não criou commits
```

O ficheiro `kuteka-sprint-b-p0.patch` contém os commits desde `bf98a700` até `400b473e` (format-patch).

## Opção 2 — já tinhas B base (`8faa18b6`) e só falta harden
```bash
git apply kuteka-sprint-b-p0-harden.patch
```

## Opção 3 — A+B de uma vez desde main `ce203d4`
```bash
git apply kuteka-sprint-ab-combined.patch
```
Preferível só se A ainda não estiver em review separado.

## Validação
Ver `REGRESSION-LOCAL.md`.

## Não aplicar sem Founder
`docs/engineering/proposals/0043_beta_feedback_founder_read_access.sql`
